<%   @menu_page = :items %>
<%= render 'account/account_nav' %>
<div id="internal-right">
	<div id="inventory-menu">
		<div id="inventory-menu-leftarrow" class="disabled"></div>
        <div id="inventory-menu-rightarrow"></div>
		<div id="inventory-boxdisplay">
            <div id="inventory-boxcanvas">
				<div class="inventory-boxdisplay-box <%= params[:box_id].blank? ? "activebox" : "" %>">
                   <p><a href="/stored_items">SHOW ALL</a></p>
                   <p>content of all boxes</p>
                   <div class="inventory-boxdisplay-image showall"><a href="/stored_items"></a></div>
              	</div>
	<% @boxes.each do |box| %>
				<div class="inventory-boxdisplay-box <%= box.id.to_s == params[:box_id] ? "activebox" : "" %>">
			        <p><a href="/boxes/<%= box.id %>/stored_items">Box <%= box.id %></a></p>
			        <p>
					<% if box.description.blank? %>
						<i>no description</i>
					<% else %>
						<%= box.description %>
					<% end %>
					</p>
			        <div class="inventory-boxdisplay-image"><a href="/boxes/<%= box.id %>/stored_items"></a></div>
			    </div>
	<% end %>
				<div class="clearer"></div>
			</div>
		</div>
	</div>
	<div class="graybox">
		<div>
<!--            <p>Filters:  <a href="#">Date Added</a> | <a href="#">Item size</a> | <a href="#">Alphabetical Order</a></p>
            <br /> -->
        </div>
        <div>
			<ul id="create-portfolio" class="thumbnails"> 
			<% @stored_items.each_with_index do |stored_item, index| %>
			<li>
				<div class="post-thumbnail">
					<a href="#"><%= image_tag(stored_item.photo.url(:medium), :name => "medium_" + stored_item.id.to_s) %></a>
			<ul>
				<li><a href="#">Sell</a></li>
				<div class="secondchild"><li><a href="#">Donate</a></li></div>
				<div class="thirdchild"><li><a href="#">Dispose</a></li></div>
				<div class="fourthchild"><li><a href="#">Mail</a></li></div>					
			</ul>
				</div>
            <p>
			Box: <%= stored_item.box_id %>; Stored Item: <%= stored_item.id %><br>
				<!--<div id="stored_item_tags_<%= stored_item.id %>" style="display: inline">
				Tags: 
				<% stored_item.stored_item_tags.each_with_index do |tag, index| %>
				<% divider = (index == 0 ? "" : "| ") %>
				<div id="tag_<%= tag.id %>" style="display: inline"><%= divider %><%=h tag.tag %> <%= link_to("X", "/stored_item_tags/#{tag.id}/delete", :method => :delete, :remote => true, :title => "delete this tag") %></div>
				<% end %>
			</p>
			</div>
			<p class="add-tags">
				<div id="add_tags_link_<%= stored_item.id %>"><a href="javascript:showTagsForm('<%= stored_item.id %>')">+add tags</a></div>
				<div id="add_tags_form_<%= stored_item.id %>" style="display: none">
					<%= form_tag( { :controller => "stored_item_tags", :action => "add_tag"}, { :remote => true, :name => "tags_form", :id => "tags_form_#{stored_item.id}" } ) do %>
					<%= hidden_field_tag :stored_item_id, stored_item.id %>
					<%= text_field_tag("tag")%>
					<%= submit_tag("Add") %> <a href="javascript:hideTagsForm('<%= stored_item.id %>')">(done)</a>
					<% end %> 
				</div>
			</p>-->
			</li>
				<% if (index - 3) % 4 == 0 %>
				<div class="clearer"></div>
				<% end %>

			<% end %>
			</ul>
		</div>
		<div class="clearer"></div>
		<!--
        <div class="inventory-pagination">
            <a href="#" class="active">1</a>
            <a href="#">2</a>
            <a href="#">3</a>
            <a href="#">4</a>
            <a href="#">5</a>
        </div>
		-->
	</div>
</div>
<div class="clearer"></div>

