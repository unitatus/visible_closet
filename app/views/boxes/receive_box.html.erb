<% @admin_page = :receiving %>
<%= render :partial => "admin/admin_menu", :locals => { :admin_page => @admin_page } %>
<div id="internal-right">
	<div class="login-slim-centered">
	<div class="simple_header">Box Receiving</div>
		<% if @messages.size > 0 %>
			<ul>
				<% @messages.each do |message| %>
					<li><%= message %></li>
				<% end %>
			</ul>
		<% end %>
		
		<% if @error_messages.size > 0 %>
			<div class="error_label">
				<ul>
					<% @error_messages.each do |error_message| %>
						<li><%= error_message %></li>
					<% end %>
				</ul>
			</div>
		<% end %>		
		<br><br><br>
		<%= form_tag "", :class => "standard", :id => "box_form", :onsubmit => "cf_submit.disabled=true;" do %>
			<div class="field clearfix">
				Box ID:
				<%= text_field_tag :box_id, "", :class => "textbox", :value => params[:box_id] %>
			</div>
			<div class="field clearfix">
				Box dimensions (w/l/h) in INCHES:
				<%= text_field_tag :width, "", :class => "textbox", :style => "width: 6", :placeholder => "width", :value => params[:width] %>
				<%= text_field_tag :length, "", :class => "textbox", :style => "width: 6", :placeholder => "length", :value => params[:length] %>
				<%= text_field_tag :height, "", :class => "textbox", :style => "width: 6", :placeholder => "height", :value => params[:height] %> 
			</div>
			<div class="field clearfix">
				Box weight in lbs:
				<%= text_field_tag :weight, "", :class => "textbox", :style => "width: 6", :placeholder => "weight", :value => params[:weight] %>&nbsp;
			</div>
			<div class="field clearfix">
				Box marked for indexing:
				<%= check_box_tag :marked_for_indexing, "1", @marked_for_indexing_locked, :disabled => @marked_for_indexing_locked %>
			</div>
			<div class="field clearfix">
				Lock indexing checkbox
				<%= check_box_tag :marked_for_indexing_locked, "1", @marked_for_indexing_locked, :onclick => "ToggleIndexingLock();"  %>
			</div>
			<div class="field clearfix">
				<%= submit_tag :submit, :class => "button", :id => "cf_submit", :onclick => "return formMonitor();" %>
			</div>
		<% end %>
		<br>
<h5>This page is used for rapid-fire scanning of box id's. Use the checkbox to indicate that a box was indicated for invoicing. If a box was specified for invoicing and you do not select the checkbox the box will still be invoiced -- users who selected invoicing online have already committed to it, so you can't turn it off here (they have to explicitly turn it off).<br><br>To scan a series of boxes that are all marked for indexing, click the "index indicator lock" checkbox.		
<script type="text/javascript" language="JavaScript">
document.forms['box_form'].elements['box_id'].focus();
</script>
</div>
</div>
<div class="clearer"></div>
