<%= form_for(@profile, :url => "/payment_profiles", :html => { :class => "registration" }) do |f| %>
<%= render :partial => 'form_cc_guts', :locals => { :f => f, :profile => @profile } %>
<div style="text-align: right; margin-right: 150px"><br>
		<%= f.submit "Save", :class => "button" %>
</div>
</div>
<br><br>
<% end %>